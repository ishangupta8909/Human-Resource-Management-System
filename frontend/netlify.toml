[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Set VITE_API_URL in Netlify UI to point to your backend (e.g. https://api.example.com)
  VITE_API_URL = "$VITE_API_URL"
